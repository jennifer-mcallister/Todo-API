type Todo {
	id: ID
	task: String
	description: String
	done: Boolean
}

type DeletedResourceResponse {
	deletedId: ID
	success: Boolean
}

type Query {
	getTodoById(id: ID!): Todo
	getAllTodos: [Todo]
}

type Mutation {
	createTodo(task: String!, description: String): Todo
	updateTodo(id: ID!, task: String!, done: Boolean!): Todo
	deleteTodo: DeletedResourceResponse
}